spring:
  application:
    name: ReverseGanttChart=
  datasource:
    url: jdbc:postgresql://localhost:54321/storage
    password: password
    driver-class-name: org.postgresql.Driver
    username: user

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      password: password
      timeout: 2000ms


jwt:
  cookie-token-key: '{"kty":"oct","k":"956PBFst1FguLr5irJD-aQ"}'
logging:
  level:
    by.pirog.ReverseGanttChart: DEBUG
    org.springframework.web: DEBUG
    org.hibernate: DEBUG

app:
  security:
    token:
      blacklist-token-project-ttl: 2d
      blacklist-token-project-prefix: "blacklist:project:"
      blacklist-token-auth-ttl: 7d
      blacklist-token-auth-prefix: "blacklist:auth:"
    cookie:
      project-cookie-name: "__Host-project-token"
      auth-cookie-name: "__Host-auth-token"